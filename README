# AI VISION  Backend API

This is a simple FastAPI backend API with endpoints for user authentication and management.

## Features

- User Signup: Register a new user.
- User Login: Authenticate and receive an access token.
- Get User: Retrieve user details.
- Delete User: Delete a user account.
- Upload Image -- Return the Class of the Image
- Get Available Imageg -- return all the list of available image

## Getting Started

### Installation

1. Clone the repository:

   ```bash
   git clone https://github.com/ImonEmmanuel/AiVision.git
   cd AiVision

   
   ```Install dependencies

    pip install -r requirements.txt
    Run the FastAPI app:

    ```bash
    uvicorn main:app --reload

Contributing

Contributions are welcome! Fork the repository, make your changes, and submit a pull request.